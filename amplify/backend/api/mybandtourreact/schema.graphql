type Usuarios @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: String!
  password: String
  tipo: String
}

type evento @model @auth(rules: [{allow: public}]) {
  id: ID!
  pais: String
  banda: String
  lugar: String
}
 